---
trigger: model_decision
description: When writing or updating interactors
---

# Input and output data
- Interactor always returns one object. It can't return tuple. If you need to return multiple values, add DTO in application layer